# Arm C' — Π-guidance with probe-CENTERED V: Π built only from changes in the
# relative geometry of the probes (mean-norm and per-probe offset shifts in ΔK
# removed). Ablation of arm C; same everything else.
_base_: arm_c_pi_only.yaml
plasticity: {kernel: centered}
wandb: {name: av-armC-pi-centered-vnorm}
train: {outdir: runs/av-armC-centered-vnorm}
